# 明文存储凭据，不引入安全层

CLI 运行在安全的内网环境，唯一消费者是长期自主运行的 agent，效率是第一优先级。因此凭据（endpoint、refreshToken、**密码**）明文存储在 `~/.hostbee/` 配置文件中，不做加密、不做密钥环集成；密码保留是为了 refreshToken 失效时能自动重新登录。禁止任何为安全性而增加代码复杂度的设计（如 token 加密存储、安全提示、权限分层）——agent 持有 admin 账号，命令面对全 schema 扁平开放，操作边界由后端权限决定，不在 CLI 侧设防。